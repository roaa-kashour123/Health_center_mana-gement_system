@startuml
skinparam defaultFontName Tajawal
start
:عرض نموذج تسجيل الدخول (\`login.php\`);
:إدخال البريد الإلكتروني وكلمة المرور;
if (البيانات صالحة (Required)؟) then (نعم)
  :الاتصال بقاعدة البيانات;
  :استدعاء دالة \`loginByEmail()\` في \`Admin\` Model;
  if (تم إيجاد المستخدم ومطابقة كلمة المرور؟) then (نعم)
    :إنشاء جلسة \`Session\` للمستخدم;
    :إعادة توجيه إلى لوحة التحكم;
    stop
  else (لا)
    :عرض رسالة "بيانات الاعتماد غير صحيحة";
  endif
else (لا)
  :عرض رسالة "الرجاء إدخال الحقول المطلوبة";
endif
:إعادة المحاولة;
@enduml